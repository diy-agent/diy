[project]
name = "diy-llm"
version = "0.1.0"
description = "diy-llm — 配置同步: provider → PI agent / Hermes"
requires-python = ">=3.12"
dependencies = [
    "cyclopts>=3.0",
]

[project.scripts]
diy-llm = "diy_llm.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/diy_llm"]

[tool.hatch.build.targets.sdist]
include = [
    { path = "src/diy_llm/providers/**/provider.yaml", format = "wheel" },
]

[tool.uv]
package = true
